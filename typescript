// src/lib/procurementUtils.ts
<<<<<<< SEARCH
    .filter(item => item.type === "raw") // Only raw materials
    .map(item => {
      const shortage = calculateShortage(
        item.orderRequired,
        item.available,
        item.reserved,
        item.minStockLevel
      );
      const purchasingQty = item.purchasingQty > 0 ? item.purchasingQty : shortage;

      return {
        ...item,
        shortage,
        purchasingQty,
        estCost: purchasingQty * (item.vendorPrice || 0)
      };
    })
    .filter(item => item.shortage > 0); // Only show items with shortages
=======
    .filter(item => item.type === "raw") // Only raw materials
    .map(item => {
      const shortage = calculateShortage(
        item.orderRequired,
        item.available,
        item.reserved,
        item.minStockLevel
      );
      const purchasingQty = item.purchasingQty > 0 ? item.purchasingQty : shortage;

      return {
        ...item,
        shortage,
        purchasingQty,
        estCost: purchasingQty * (item.vendorPrice || 0)
      };
    }); // Temporarily removed filter to show all raw materials
>>>>>>> REPLACE
