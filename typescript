    gradePlanning: v.optional(
      v.array(
        v.object({
          grade: v.string(), // "1", "2", etc.
          month: v.string(), // "April", "May", etc.
          kitId: v.id("kits"),
          // ... other fields
        })
      )
    ),
