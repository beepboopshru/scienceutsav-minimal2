    if (productionMonth && dispatchDate) {
      const prodDate = new Date(productionMonth + "-01");
      const dispDate = new Date(dispatchDate);
      if (prodDate > dispDate) {
        toast.error("Production month must be before or same as dispatch month");
        return;
      }
    }
